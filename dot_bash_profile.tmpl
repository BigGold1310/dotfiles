{{ if eq .chezmoi.os "darwin" -}}
eval "$(/opt/homebrew/bin/brew shellenv)"
{{ end -}}
# Source .bashrc for interactive settings (login shells skip .bashrc by default)
[[ -f "${HOME}/.bashrc" ]] && source "${HOME}/.bashrc"
